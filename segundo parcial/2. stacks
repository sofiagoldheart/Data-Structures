#include <iostream>
using namespace std;

struct Stacki{

  int sizestack;
  int top;
  int *arri;
};

bool InitStacki(Stacki *sti, int s);
void ShowStack(Stacki sti);
bool isStackiEmpty (Stacki si);
bool isStackFull (stacki si);

int main() {

  cout << "Creador de pilas!\n";

  Stacki s1;
  if(!InitStacki(&s1,3)){
    cout << "Error" << endl;
    exit(1);
      }
      ShowStack(s1);   
      }
 bool InitStacki (Stacki *sti, int s){
      if ((sti->arri = new int[s]) == NULL){
        return false;  
      }
      sti->top=-1;
      sti -> sizestack = s;
      return true;
    }
    
 void ShowStack(Stacki sti){
  cout << "Posicion: " << sti.top << endl;
  cout << "Tamaño del stack: " <<sti.sizestack; 
  }
  
  bool isStackiEmpty (Stacki si){
    if (si.top == -1)
      return true;
    }
    return false;
    }
    
  bool isStackFull (stacki si){
      if (si top == ()si.sstack -1) ) {
  return true;
    }
    return false;
    }  
    
   bool Pushi (Slacki *si, int elem){
    int (isStackiFull (*(si))){
      return false;
    }
    si -> arii [++si -> top] = elem;
      return true;
    }
